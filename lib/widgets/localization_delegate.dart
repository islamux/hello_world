import 'package:flutter/material.dart';

class LocalizationDelegate extends StatelessWidget {
  const LocalizationDelegate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
